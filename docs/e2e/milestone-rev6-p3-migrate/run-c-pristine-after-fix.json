{
  "env": { "userData": "$SCR/migrate2/ud = a fresh `bun run fixture:legacy-land` output + part A's identity/ (same device key) + inference.json {kind: llamacpp, baseUrl: http://127.0.0.1:8859/v1, model: none, apiKeyEnv: null, sidecar: null}", "app": "OPENAI_API_KEY= THESYS_API_KEY= AETHER_TEST_USER_DATA=$SCR/migrate2/ud bun run dev --remoteDebuggingPort 9351", "driver": "CDP_PORT=9351", "build": "after this session's planner fix (src/dsl/history/migrateLand.ts)" },
  "model": "none",
  "actions": [
    { "note": "C1: the display name part A migrated with, so the profile event is the same" },
    { "wait": 1500 },
    { "eval": "(async () => { const m = await import(\"/net/room.ts\"); m.setPlayerName ? m.setPlayerName(\"player-CP8P\") : null; return m.playerName() })()" },
    { "clickText": "設定" },
    { "wait": 800 },
    { "clickText": "English" },
    { "wait": 600 },
    { "hold": "Escape", "ms": 80 },
    { "wait": 600 },
    { "clickText": "Continue" },
    { "wait": 3500 },
    { "text": true },
    { "shot": "docs/e2e/milestone-rev6-p3-migrate/c-00-pristine-rerun-after-fix.jpg" },
    { "clickText": "← Home" },
    { "wait": 1200 }
  ]
}
