{
  "env": {
    "app": "AETHER_TEST_USER_DATA=$TMPDIR/ud-followup-signaling (fresh, empty) bun run dev --remoteDebuggingPort 9335 — CDP_PORT=9335 for every phase",
    "signaling": "PORT=4444 node node_modules/y-webrtc/bin/server.js (y-webrtc's bundled server) running for the whole run",
    "silent": "PORT=4446 node <scratch>/silent.mjs before `actions`: a TCP server that accepts and never answers, so a WebSocket handshake stays pending (the symptom the Fly server showed). Source: net.createServer((s) => { s.on('error', () => {}) }).listen(4446, '127.0.0.1')",
    "locale": "zh-TW (system default)",
    "window": "1440 × 868",
    "build": "main at 70f5eba + the uncommitted rev 6 working tree incl. the signaling follow-up (bun run dev)",
    "order": "actions → world → shell swap4446 → recover → shell stop4446 → lost → fix → language → defaultDoor",
    "shell": {
      "swap4446": "stop the silent server on 4446, then PORT=4446 node node_modules/y-webrtc/bin/server.js &",
      "stop4446": "stop the y-webrtc server on 4446 (the one started in swap4446)"
    },
    "replay": "CDP_PORT=9335 bun scripts/cdp-drive.ts \"$(bun -e 'console.log(JSON.stringify(require(\"./docs/e2e/milestone-rev6-followup-signaling/run.json\").<phase>))')\""
  },
  "model": "openai · gpt-5.4-mini (one call: witnessing the new world's origin chunk)",
  "actions": [
    { "clickText": "系統" },
    { "wait": 1200 },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "eval": "[document.querySelector('textarea').value, localStorage.getItem('unwritten.signaling'), ...[...document.querySelectorAll('button')].filter(b=>/測試連線|儲存到這台裝置|使用預設/.test(b.innerText)).map(b=>b.innerText+':'+(b.disabled?'disabled':'enabled'))]" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/01-system-default-list.jpg" },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<400;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return [Math.round(performance.now()-t0), s.slice(i,i+600)]})()" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/02-default-tested.jpg" },
    { "eval": "(()=>{const t=document.querySelector('textarea');t.focus();t.select();return t.value})()" },
    { "type": "ws://127.0.0.1:4444" },
    { "wait": 300 },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<400;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return [Math.round(performance.now()-t0), s.slice(i,i+200)]})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/03-local-tested.jpg" },
    { "clickText": "儲存到這台裝置" },
    { "wait": 400 },
    { "eval": "[localStorage.getItem('unwritten.signaling'), (()=>{const s=document.body.innerText;const i=s.indexOf('信令伺服器\\n');return s.slice(i,i+400)})()]" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/04-local-saved.jpg" },
    { "eval": "(()=>{const t=document.querySelector('textarea');t.focus();t.select();return t.value})()" },
    { "type": "ftp://nope" },
    { "wait": 300 },
    { "eval": "[(()=>{const s=document.body.innerText;const i=s.indexOf('信令伺服器\\n');return s.slice(i,i+330)})(), ...[...document.querySelectorAll('button')].filter(b=>/測試連線|儲存到這台裝置|使用預設/.test(b.innerText)).map(b=>b.innerText+':'+(b.disabled?'disabled':'enabled'))]" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/05-bad-url-malformed.jpg" },
    { "eval": "(()=>{const t=document.querySelector('textarea');t.focus();t.select();return t.value})()" },
    { "type": "ws://127.0.0.1:1\nwss://echo.websocket.org" },
    { "wait": 300 },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<400;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return [Math.round(performance.now()-t0), s.slice(i,i+1000)]})()" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/06-bad-url-tested.jpg" },
    { "eval": "localStorage.getItem('unwritten.signaling')" },
    { "eval": "(()=>{const t=document.querySelector('textarea');t.focus();t.select();return t.value})()" },
    { "type": "ws://127.0.0.1:4446" },
    { "wait": 300 },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<400;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return [Math.round(performance.now()-t0), s.slice(i,i+400)]})()" },
    { "clickText": "儲存到這台裝置" },
    { "wait": 400 },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/07-silent-server-saved.jpg" },
    { "eval": "localStorage.getItem('unwritten.signaling')" }
  ],
  "world": [
    { "hold": "Escape", "ms": 60 },
    { "wait": 800 },
    { "clickText": "新遊戲" },
    { "wait": 2500 },
    { "eval": "(()=>{const i=document.querySelectorAll('input')[0];i.focus();i.select()})()" },
    { "type": "SGNL-7K4R" },
    { "clickText": "開始" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<480;i++){if(/已記/.test(document.body.innerText))return [Math.round(performance.now()-t0), document.body.innerText.slice(0,200)];await new Promise(r=>setTimeout(r,250));}return 'still writing';})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/08-new-world.jpg" },
    { "hold": "KeyE", "ms": 80 },
    { "wait": 1200 },
    { "eval": "(()=>{window.__hud=[];window.__open=performance.now();clearInterval(window.__poll);window.__poll=setInterval(()=>{const m=(document.body.innerText.match(/大陸 [A-Z0-9]{6} ·[^\\n]*/g)||[]).join(' | ');const last=window.__hud[window.__hud.length-1];if(!last||last[1]!==m)window.__hud.push([Math.round(performance.now()-window.__open),m]);},100);return 1})()" },
    { "clickText": "向夥伴敞開我的門" },
    { "eval": "window.__open=performance.now(); 1" },
    { "wait": 1500 },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/09-door-open-connecting.jpg" },
    { "clickText": "關閉" },
    { "wait": 2000 },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/10-hud-connecting.jpg" },
    { "eval": "(async()=>{while(performance.now()-window.__open<12800)await new Promise(r=>setTimeout(r,100));return JSON.stringify(window.__hud)})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/11-hud-connecting-continued.jpg" },
    { "eval": "(async()=>{for(let i=0;i<200;i++){if(window.__hud.some(h=>/沒有任何信令伺服器/.test(h[1])))break;await new Promise(r=>setTimeout(r,200));}return JSON.stringify(window.__hud)})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/12-hud-error.jpg" },
    { "hold": "KeyE", "ms": 80 },
    { "wait": 1200 },
    { "eval": "(()=>{const s=document.body.innerText;const i=s.search(/大陸 [A-Z0-9]{6} —/);return s.slice(i,i+600)})()" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&/^大陸 [A-Z0-9]{6} —/.test(e.innerText.trim())&&e.children.length===0);if(h)h.scrollIntoView({block:'center'});return !!h})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/13-door-panel-error.jpg" },
    { "clickText": "關閉" },
    { "wait": 600 },
    { "eval": "window.__swap=performance.now(); Math.round(window.__swap-window.__open)" }
  ],
  "recover": [
    { "eval": "(async()=>{for(let i=0;i<300;i++){if(window.__hud.some(h=>/已連線/.test(h[1])))break;await new Promise(r=>setTimeout(r,200));}return [Math.round(performance.now()-window.__swap), JSON.stringify(window.__hud)]})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/14-hud-live-recovered.jpg" },
    { "eval": "window.__lost=performance.now(); Math.round(window.__lost-window.__open)" }
  ],
  "lost": [
    { "eval": "(async()=>{const n=window.__hud.length;for(let i=0;i<300;i++){if(window.__hud.slice(n).some(h=>/沒有任何信令伺服器/.test(h[1])))break;await new Promise(r=>setTimeout(r,200));}return JSON.stringify(window.__hud.map(h=>[h[0]-Math.round(window.__lost-window.__open),h[1].slice(0,40)]))})()" }
  ],
  "fix": [
    { "clickText": "← 主頁" },
    { "wait": 1500 },
    { "clickText": "系統" },
    { "wait": 1200 },
    { "eval": "(()=>{const t=document.querySelector('textarea');t.focus();t.select();return t.value})()" },
    { "type": "ws://127.0.0.1:4444" },
    { "wait": 300 },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{for(let i=0;i<200;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return s.slice(i,i+80)})()" },
    { "clickText": "儲存到這台裝置" },
    { "wait": 400 },
    { "eval": "localStorage.getItem('unwritten.signaling')" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/15-system-local-saved-again.jpg" },
    { "hold": "Escape", "ms": 60 },
    { "wait": 800 },
    { "clickText": "繼續遊戲" },
    { "wait": 3000 },
    { "hold": "KeyE", "ms": 80 },
    { "wait": 1200 },
    { "eval": "(()=>{window.__hud=[];window.__open=performance.now();clearInterval(window.__poll);window.__poll=setInterval(()=>{const m=(document.body.innerText.match(/大陸 [A-Z0-9]{6} ·[^\\n]*/g)||[]).join(' | ');const last=window.__hud[window.__hud.length-1];if(!last||last[1]!==m)window.__hud.push([Math.round(performance.now()-window.__open),m]);},100);return 1})()" },
    { "clickText": "向夥伴敞開我的門" },
    { "eval": "window.__open=performance.now(); 1" },
    { "wait": 800 },
    { "clickText": "關閉" },
    { "eval": "(async()=>{for(let i=0;i<100;i++){if(window.__hud.some(h=>/已連線/.test(h[1])))break;await new Promise(r=>setTimeout(r,200));}return JSON.stringify(window.__hud)})()" },
    { "wait": 500 },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/16-hud-live-local.jpg" }
  ],
  "language": [
    { "clickText": "← 主頁" },
    { "wait": 1500 },
    { "clickText": "系統" },
    { "wait": 1200 },
    { "clickText": "English" },
    { "wait": 800 },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='Signaling servers');h.scrollIntoView({block:'start'});const s=document.body.innerText;const i=s.indexOf('Signaling servers\\n');return s.slice(i,i+520)})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/17-panel-en.jpg" },
    { "clickText": "日本語" },
    { "wait": 800 },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='シグナリングサーバー');h.scrollIntoView({block:'start'});const s=document.body.innerText;const i=s.indexOf('シグナリングサーバー\\n');return s.slice(i,i+400)})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/18-panel-ja.jpg" },
    { "clickText": "繁體中文" },
    { "wait": 800 },
    { "clickText": "使用預設" },
    { "wait": 500 },
    { "eval": "[localStorage.getItem('unwritten.signaling'), document.querySelector('textarea').value, (()=>{const s=document.body.innerText;const i=s.indexOf('信令伺服器\\n');return s.slice(i,i+360)})(), ...[...document.querySelectorAll('button')].filter(b=>/測試連線|儲存到這台裝置|使用預設/.test(b.innerText)).map(b=>b.innerText+':'+(b.disabled?'disabled':'enabled'))]" },
    { "clickText": "測試連線" },
    { "eval": "(async()=>{const t0=performance.now();for(let i=0;i<400;i++){await new Promise(r=>setTimeout(r,100));if(!/測試中…/.test(document.body.innerText))break;}const s=document.body.innerText;const i=s.indexOf('使用預設');return [Math.round(performance.now()-t0), s.slice(i,i+520)]})()" },
    { "eval": "(()=>{const h=[...document.querySelectorAll('*')].find(e=>e.innerText&&e.innerText.trim()==='信令伺服器');h.scrollIntoView({block:'start'});return 1})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/19-back-to-default-tested.jpg" }
  ],
  "defaultDoor": [
    { "hold": "Escape", "ms": 60 },
    { "wait": 800 },
    { "eval": "localStorage.getItem('unwritten.signaling')" },
    { "clickText": "繼續遊戲" },
    { "wait": 3000 },
    { "hold": "KeyE", "ms": 80 },
    { "wait": 1200 },
    { "eval": "(()=>{window.__hud=[];window.__open=performance.now();clearInterval(window.__poll);window.__poll=setInterval(()=>{const m=(document.body.innerText.match(/大陸 [A-Z0-9]{6} ·[^\\n]*/g)||[]).join(' | ');const last=window.__hud[window.__hud.length-1];if(!last||last[1]!==m)window.__hud.push([Math.round(performance.now()-window.__open),m]);},100);return 1})()" },
    { "clickText": "向夥伴敞開我的門" },
    { "eval": "window.__open=performance.now(); 1" },
    { "wait": 800 },
    { "clickText": "關閉" },
    { "eval": "(async()=>{for(let i=0;i<150;i++){if(window.__hud.some(h=>/已連線|沒有任何/.test(h[1])))break;await new Promise(r=>setTimeout(r,200));}await new Promise(r=>setTimeout(r,3000));return JSON.stringify(window.__hud.map(h=>[h[0],h[1].slice(0,40)]))})()" },
    { "shot": "docs/e2e/milestone-rev6-followup-signaling/20-hud-default-servers.jpg" }
  ]
}
