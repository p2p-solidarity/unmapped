// The ZIP central-directory check is pure and shared (@shared/archive, rev 6 phase 4), with the
// bounded reader every received pack goes through.
export { validateArchiveEntryNames } from "@shared/archive";
